//
//  SlideVC.swift
//  Townhero
//
//  Created by Pasha Bahadori on 6/30/16.
//  Copyright © 2016 Mohamed. All rights reserved.
//

import UIKit

class SlideVC: UIViewController {

    @IBOutlet var slideSegueButton: UIButton!
    
    
    override func viewDidLoad() {
        super.viewDidLoad()

        
    }


    override func prepareForSegue(segue: UIStoryboardSegue, sender: AnyObject?) {
        if segue.identifier == "SlideSegueToProfile" {
            let profileVC = segue.destinationViewController as! ProfileTVC
            
//            if segue.identifier == "editEmailSegue"{
//                let dvc = segue.destinationViewController as! EditEmailViewController
//                dvc.passedEmail = self.editProfileEmail.text
        }
    }
    
    @IBAction override func unwindForSegue(unwindSegue: UIStoryboardSegue, towardsViewController subsequentVC: UIViewController) {
        
    }
    

    

    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepareForSegue(segue: UIStoryboardSegue, sender: AnyObject?) {
        // Get the new view controller using segue.destinationViewController.
        // Pass the selected object to the new view controller.
    }
    */

}
